# Runtime-Specific Skill Extensions

Some runtimes add extra frontmatter fields or environment variables beyond the base [Agent Skills specification](https://agentskills.io/specification). Treat these as optional. Prefer portable skills unless the repository intentionally depends on a specific runtime.
